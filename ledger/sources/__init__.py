"""Source adapters: sense one class of source and emit compatibility observations.

Adapters are read-only toward source projects and must not decide canonical identity.
They are invoked through the ``SourceAdapter`` abstraction in ``base.py``.
"""